<form method="post" action="{{ path('app_ficha_delete', {'idFicha': ficha.idFicha}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ficha.idFicha) }}">
    <button class="btn">Delete</button>
</form>
